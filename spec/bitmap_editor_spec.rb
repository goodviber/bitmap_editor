RSpec.describe BitmapEditor do
  it "has a version number" do
    expect(BitmapEditor::VERSION).not_to be nil
  end

  it "does something useful" do
    byebug
    expect(false).to eq(true)
  end
end
